---
description: Clone and manage local reference repositories under .ref/ for source exploration (the reference-repos skill).
globs:
alwaysApply: false
---

# reference-repos

Use the shared repo skill to clone and manage upstream reference repositories under
`.ref/` for source exploration and published-library context.

- Skill body + full command reference: `.cursor/skills/reference-repos/SKILL.md`
  (symlink to the canonical `plugins/reference-repos/skills/reference-repos/`).
- Run the bundled CLI from the repo root:
  `.cursor/skills/reference-repos/ref-repos <command>` (`.bat` on Windows).
- Operational rules and gotchas (sequential `add`, manifest recovery, branch pins):
  see `AGENTS.md`.
